# dbt
target/
dbt_packages/
logs/
.user.yml

# Python
.venv/
__pycache__/
*.pyc

# Credentials — keys live in ~/.secrets/, never in repo
*.json
!package-lock.json
.env
# profiles.yml IS committed; it contains only env_var() references, no secrets.

# Editor
.vscode/
.idea/
*.swp
.DS_Store
