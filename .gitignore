# dbt
target/
dbt_packages/
logs/
.user.yml

# Python
.venv/
__pycache__/
*.pyc

# Credentials — keys live in ~/.secrets/, never in repo
*.json
!package-lock.json
.env
# profiles.yml lives at ~/.dbt/profiles.yml (standard dbt location), not in repo.

# Editor
.vscode/
.idea/
*.swp
.DS_Store
