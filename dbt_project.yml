name: cfpb_complaints
version: 0.1.0
config-version: 2

profile: cfpb_complaints

models:
  cfpb_complaints:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
